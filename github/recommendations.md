# GitHub — Recommendations for Mohammed Shabaz S

Feeds [`../synthesis/github-blueprint.md`](../synthesis/github-blueprint.md). Grounded in the candidate's actual repos + recruiter signals.

## 1. Pins (choose 3–4, in this order)
`[REC, High]` **Automotive BCM (ESP32)** → **Smart Wellness (STM32, team)** → **VisionPay (Python/CV)**. These are real, finished, and on-theme (embedded + on-device CV). **Do not pin** the `aegis` skeleton (empty) or NexCast_Pro (removed from profile).

## 2. Highest-impact, lowest-cost fixes (do first)
`[REC, High]`
1. **Set repo descriptions + topics** on all three (currently null). Instant recruiter-scan improvement.
2. **Upgrade each README** to the [`repository-framework.md`](repository-framework.md) Strong standard — the reference repo already contains the real hardware photo (BCM), the wiring diagram (Smart Wellness), and honest project copy; move that content into the repos.
3. **VisionPay language fix** — foreground `.py` modules over the notebook.

## 3. Profile README (`MdShabazS/MdShabazS`)
`[REC, High]` Keep it tight and honest: identity (embedded-primary), current goal, 2–3 real capabilities with evidence, links. The reference repo's `github/profile-readme.md` is content-rich but (per the audit) contradicts its own research by using a badge wall + stat widget + layout tables; recruiters ignore those. Trim toward signal: real project image cards + honest text > shields. Fix the present-tense Nokia line to match its true lifecycle.

## 4. Elite-tier moves (higher effort, real payoff)
`[REC, Medium]`
- Add **tests + a GitHub Actions CI** to one embedded or the VisionPay repo.
- Add an **architecture/wiring diagram** and a short "engineering decisions" section to each embedded README.
- Make **one small real OSS contribution** (docs/bug fix in a library you use — TFLite examples, an Arduino/ESP library). One genuine external contribution outranks many solo repos.

## 5. Honesty guardrails
`[REC, High]` No fake commits/streaks, no empty-repo flagships, team projects labeled as team, ~93% shown as reported, MITRA not published (company-confidential). Never optimize the contribution graph — recruiters discount it.

## 6. Target
`[REC, Medium]` Near-term realistic tier: **Strong** (achievable in a weekend of README/description/diagram work). **Elite** after CI + one OSS contribution.
