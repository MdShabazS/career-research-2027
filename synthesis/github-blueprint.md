# GitHub Blueprint (spec)

Derived from [`../github/`](../github/).

## Profile README (`MdShabazS/MdShabazS`)
Tight, honest: identity (embedded-primary + CV), current goal, 2–3 real capabilities with evidence, links. Trim badge walls/stat widgets (recruiters ignore them). Nokia line at true lifecycle state.

## Pins (3–4, ordered)
Automotive BCM → Smart Wellness (team) → VisionPay. **Do not pin** aegis (empty) or NexCast_Pro (removed).

## Per-repo standard (do first — cheapest, highest impact)
1. Set **description + topics** on all three (currently null).
2. README to the Strong standard: one-line purpose → screenshot/GIF → features → stack → run steps → decisions → honest contribution.
3. Move real media into repos (BCM photo, wiring diagram, VisionPay frames).
4. VisionPay: foreground `.py` modules over the notebook.

## Elite moves (second wave)
Tests + GitHub Actions CI on one repo; architecture/wiring diagrams; one small real OSS contribution.

## Honesty firewall
No fake commits/streaks; no empty-repo flagship; team labeled team; ~93% reported; MITRA not published. Never optimize the contribution graph.

## Target
Strong (weekend of README/description/media work); Elite after CI + one OSS contribution.
