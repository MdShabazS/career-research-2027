# Gap Analysis — market/benchmark vs the candidate

Candidate's current profile (reference repo) measured against the benchmarks and market evidence. Gaps ranked by value. `[FACT]`/`[INFER]`/`[REC]` inline.

## Strengths (real, keep leveraging)
`[FACT, High]`
- **Two Strong embedded artifacts on real hardware** (Automotive BCM ESP32, Smart Wellness STM32 Cortex-M4) — matches the highest-signal market advice (documented HW projects).
- **A genuine on-device CV project** (VisionPay) — a rare fresher differentiator when paired with embedded.
- **Two real internships** (Nokia incoming; iHelp/MITRA app-side) — a credibility anchor most freshers lack.
- **ECE degree + CGPA 8.38** aligned to embedded/semiconductor demand.
- **Unusually disciplined self-documentation** (the reference repo's governance/status system) — a differentiator in itself.
- **Real leadership** (IEEE CAS Vice-Chair, Robotics Club Treasurer) and activities (IEEE SPACE 2026 participant).

## Weaknesses (real, address)
`[INFER, High]`
- Positioning is **contradictory across channels** (software-first vs embedded-first) — the #1 self-inflicted gap.
- Skills are **Basic across the board**; protocol breadth is **I2C-only**; **no RTOS**, no JTAG/SWD.
- **No demo media** (videos/GIFs) — the strongest "I build" signal is missing.
- **No tests/CI** on any repo; repo **descriptions empty**.
- A **public flagship repo (AEGIS) that is an empty scaffold** — an active negative signal.
- **DSA Basic** — blocks the software door if chosen.
- Reference-repo integrity issues (Nokia future-as-present; public repo with plaintext phone) that undermine credibility if a recruiter finds them.

## Gaps ranked

### Critical (fix before any external sharing)
1. **Resolve positioning to one identity** across resume/LinkedIn/GitHub/portfolio. `[REC]` Embedded-primary + CV differentiator (evidence-backed).
2. **Fix credibility/integrity leaks** carried from the reference repo: Nokia lifecycle wording, plaintext phone in a public repo, empty AEGIS repo unlinked/private.
3. **Per-repo READMEs + descriptions + real media** on the 3 public projects — the recruiter-scan surface.

### High-value
4. **Protocol/RTOS evidence** — one UART/SPI interrupt project + one FreeRTOS port (converts the biggest skill gaps into demonstrable keywords).
5. **Demo videos/GIFs** for each featured project.
6. **Tests + CI** on one repo (Strong→Elite GitHub signal).
7. **Strong C** depth (the #1 embedded filter) — currently Basic.

### Medium-value
8. **DBMS/OS/CN Basic→Working** for CS-fundamentals rounds.
9. **VisionPay language optics** (`.py` over notebook) + deployment/demo.
10. **LinkedIn Featured + one recommendation** from an internship mentor.
11. **Certs**: obtain artifacts for the two `TO_VERIFY` certs or drop them.

### Low-value / defer
12. GCP/cloud stack (Docker/K8s/Terraform) — defer to a year-2 pivot.
13. RAG/vector-DB — defer unless AI becomes the chosen track.
14. Automotive stack (CAN/AUTOSAR/ISO 26262) — pursue **only** if committing to automotive; start with one CAN demo.

## Differentiation opportunities
`[INFER, High]`
- **"Embedded firmware + on-device intelligence"** — few freshers pair real MCU firmware with real edge-CV. This is the memorable identity.
- **Engineering-product portfolio** (interactive state machine / annotated board) — most fresher portfolios are generic templates; a restrained, evidence-led 3D site stands out precisely because it explains real work.
- **Documentation discipline** — already a strength; surfaced publicly (clean repos, architecture diagrams) it reads as engineering maturity.
