# Strategic Recommendations

The prioritized action list. Detail lives in the asset `recommendations.md` files and [`../synthesis/final-profile-strategy.md`](../synthesis/final-profile-strategy.md).

## Tier 0 — Decide & fix integrity (before any external sharing)
1. **Adopt one identity:** *Embedded Software Engineer · on-device computer vision.* Write it into the reference repo's positioning owner and cascade to all four assets. `[REC, High]`
2. **Fix credibility leaks carried from the reference repo:**
   - Nokia shown at true lifecycle state (incoming until start date, not "currently working"). 
   - Remove the plaintext phone number from every file in the (public) reference repo; keep it only in employer-submitted resume copies.
   - Make the empty **AEGIS** repo private (or unlink it); never feature an empty scaffold.
   - Remove **"REST APIs"** from LinkedIn skills (not held).

## Tier 1 — Surface the work you already have (a weekend)
3. **GitHub:** set repo descriptions + topics; upgrade the 3 project READMEs to the Strong standard; move real media (BCM photo, wiring diagram, VisionPay frames) into the repos; foreground VisionPay's `.py` modules.
4. **Demo media:** capture a short video/GIF of each featured project working — the single strongest "I build" signal, currently missing.
5. **Align headlines** across resume/LinkedIn/GitHub to the one identity.

## Tier 2 — Ship & strengthen (weeks)
6. **Portfolio v1:** fast, content-first, honest, working links — live before any heavy 3D.
7. **Close top skill gaps with artifacts:** FreeRTOS port; UART/SPI interrupt-driven multi-sensor project; tests + CI on one repo; deepen C.
8. **LinkedIn to Strong:** Featured links, one internship-mentor recommendation.

## Tier 3 — Flagship & conditional forks (later)
9. **Signature flagship:** an edge-AI-on-MCU project fusing firmware + CV; add the one signature 3D interaction to the portfolio.
10. **Conditional branches (pick deliberately, don't scatter):** CAN demo → *if automotive*; ACE cert + one cloud project → *if cloud*; a RAG demo → *if AI*.

## The through-rule
`[REC, High]` Every asset must trace to the one identity, every claim to a real artifact, and nothing may appear above its true depth. Truthfulness and consistency are the strategy — not constraints on it.
